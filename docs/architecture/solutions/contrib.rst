Contrib Modules
===============

Julee ships with contrib modules - ready-made, reusable components for common needs.
Unlike accelerators (which are full bounded contexts), contrib modules are utilities
that can be composed into your solutions.

Available Modules
-----------------

.. contrib-list::

For detailed documentation on each module, see :doc:`/domain/contrib/index`.
