"""Tests for the doctrine rules themselves.

Not under the doctrine package, because that package is what a codebase
runs against itself. A kit checking its own claims should not also be
running julee's tests.
"""
