# Maintenance

Documentation for maintainers of the julee library.

## Releasing

```bash
python -m julee.maintenance.release prepare 0.2.0
# merge PR
python -m julee.maintenance.release tag 0.2.0
```

See `releasing.rst` for PyPI setup and manual procedures.
